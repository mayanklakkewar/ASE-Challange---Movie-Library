// Re-export the movie context, provider, and custom hook from Context.jsx
// This allows other parts of the app to import them from a single file
export { movieContext, useMovie, MovieProvider } from "./Context.jsx";
